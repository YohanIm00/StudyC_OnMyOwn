/*Input with using scanf function*/
#include <stdio.h>

int main(void)
{
	int a;

	scanf("%d", &a);				// in this moment, put any value on variable a
	printf("Typed value: %d\n", a);	// print what we typed

	return 0;
}